# Sanity CMS — Manual Fixes Checklist
**Project:** Predicting Customer Churn: Selecting the Optimal ML Model
**Page URL:** https://aayushyagol.com/projects/bank-customer-churn-selecting-the-optimal-ml-model

---

## Fix A — Re-upload Broken Images

7 of 9 images on the portfolio page are failing to render (naturalWidth: 0).
Follow these steps to fix each one.

### Step 1 — Open Sanity Studio
- Go to your Sanity Studio URL (e.g. `studio.aayushyagol.com`)
- Log in if prompted

### Step 2 — Navigate to the project entry
- In the left sidebar, find **Projects** (or your content type name)
- Open: **"Predicting Customer Churn: Selecting the Optimal ML Model"**

### Step 3 — Identify broken image blocks
For each image block in the body content:
1. Click the image block
2. Check whether a file is actually attached — look for a file size > 0 bytes
3. If the image shows as empty, broken, or 0 bytes → it needs replacing

### Step 4 — Replace each broken image
For each broken image block:
1. Click the image block to select it
2. Click **Delete** (or the trash icon) to remove the block entirely
3. Position your cursor where the image was
4. Click **Insert** → **Image** (or drag-and-drop)
5. Upload the correct file from the `images/` folder in the project directory:
   `/Users/aayushyagol/Data-Code/PROJECTS/Churn Prediction Model-20.03.26/images/`

### Step 5 — Image reference table

| File to upload | Alt text to enter |
|---|---|
| `customer_churn_class_distribution.png` | Customer Churn — Class Distribution (79.6% stayed, 20.4% churned) |
| `gbm_feature_importance_top_10_predictors.png` | GBM Feature Importance — Top 10 Predictors |
| `model_leaderboard_accuracy_vs_recall_catch_rate.png` | Model Leaderboard — Accuracy vs Recall Catch Rate |
| `roc_curve_comparison_all_6_base_models.png` | ROC Curve Comparison — All 6 Base Models |
| `recall_improvement_gbm_mitigation_approaches.png` | Recall Improvement — GBM Mitigation Approaches |
| `precision_vs_recall_tradeoff_across_thresholds.png` | Precision vs Recall Tradeoff Across Decision Thresholds |
| `precision_recall_tradeoff_decision_thresholds_default_gbm.png` | Precision–Recall Tradeoff Scatter — Default GBM |

### Step 6 — Publish
- Click the **Publish** button (top right)
- Wait for confirmation that the document has been published

### Step 7 — Verify on live page
1. Open the portfolio page in an **incognito window**:
   `https://aayushyagol.com/projects/bank-customer-churn-selecting-the-optimal-ml-model`
2. Scroll through the entire page
3. Confirm every image renders — no black rectangles or blank spaces

### Step 8 — Browser console verification
Open DevTools (F12 or Cmd+Option+I) and run this in the Console tab:

```js
Array.from(document.querySelectorAll('img')).map(i => ({alt: i.alt, w: i.naturalWidth}))
```

Every image should return `w > 0`. Any image with `w: 0` is still broken.

---

## Fix B — Replace Table Image with Real Table

One content block uses a PNG image of the results table (alt: "Model Recall improvement Table").
This must be replaced with a native table so it is readable by screen readers and searchable by Google.

### Step 1 — Find the table image block
- In the Sanity Studio editor for this project, scroll to find the image block with alt text: **"Model Recall improvement Table"**
- Click it to select it

### Step 2 — Delete the image block
- Press **Delete** or click the trash icon to remove it entirely

### Step 3 — Insert a table or code block
**Option A — If your Sanity schema supports a Table block:**
1. Click **Insert** → **Table**
2. Enter the following data (5 rows + header):

| Approach | Accuracy | Churner Recall | Notes |
|---|---|---|---|
| Default GBM (baseline) | 87.0% | 48.9% | Class imbalance unaddressed |
| GBM (Sample Weighted) | 80.0% | 75.9% | Highest recall; ~7pp accuracy cost |
| SMOTE resampling | 84.7% | 66.3% | Best precision-recall balance |
| Threshold @ 0.35 | 85.8% | 60.0% | No retraining needed |
| Tuned GBM (GridSearchCV) | 86.6% | 48.2% | Imbalance limits tuning gains |

**Option B — If no Table block is available, use a Code block:**
1. Click **Insert** → **Code**
2. Paste the following markdown:

```
| Approach | Accuracy | Churner Recall | Notes |
|---|---|---|---|
| Default GBM (baseline) | 87.0% | 48.9% | Class imbalance unaddressed |
| GBM (Sample Weighted) | 80.0% | 75.9% | Highest recall; ~7pp accuracy cost |
| SMOTE resampling | 84.7% | 66.3% | Best precision-recall balance |
| Threshold @ 0.35 | 85.8% | 60.0% | No retraining needed |
| Tuned GBM (GridSearchCV) | 86.6% | 48.2% | Imbalance limits tuning gains |
```

### Step 4 — Publish
- Click **Publish** and wait for confirmation

### Step 5 — Verify on live page
1. Open the page in an incognito window
2. Right-click on where the table appears → **Inspect**
3. Confirm the element is a `<table>` tag, **not** an `<img>` tag
4. Confirm the table is readable — all 5 rows and 4 columns visible as text

---

*Generated by Claude Code — 2026-04-07*
